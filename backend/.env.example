# Solana wallet keypair as JSON array (same wallet Kaching used)
WALLET_KEYPAIR=[1,2,3,...]

# AI keys (same as Kaching — just copy them over)
GROQ_API_KEY=your_groq_key_here
ELEVENLABS_API_KEY=your_elevenlabs_key_here
ELEVENLABS_VOICE_ID=your_voice_id_here

# DO NOT fill these — auth.js generates them at runtime
# TXLINE_JWT=
# TXLINE_API_TOKEN=
